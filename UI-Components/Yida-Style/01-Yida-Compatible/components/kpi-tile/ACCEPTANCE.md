# KpiTile — ACCEPTANCE

只保留对本组件实际适用的验收项（无 hover/focus/disabled、无保存/删除/回填等交互，故不列）。

- [ ] 默认状态：label 12px 灰、value 16px/600 且颜色 = 色条色，数字等宽对齐。
- [ ] 顶部色条：3px 满宽、位于卡片顶边，随 color 变化。
- [ ] 无 color：色条回落 `#ccc`、value 回落 `rgba(0,0,0,0.88)`，不报错。
- [ ] loading：value 位显示 `—`（灰），卡片高度不跳变。
- [ ] 长标签：单行省略号，不换行、不撑破宽度。
- [ ] 多实例：同页并列多张磁贴互不干扰，`--kc` 不串色（各卡自带内联 style）。
- [ ] 尺寸可覆盖：宿主改宽度/内距后，色条仍贴顶、内容仍居中。
- [ ] 脱敏：包内无任何真实字段 ID / appType / 数据源 ID / 公司名 / 内部 URL（示例数据全为 mock）。
- [ ] 独立理解：仅凭 SPEC.md + preview.html 即可正确使用，无需回看原页面。
