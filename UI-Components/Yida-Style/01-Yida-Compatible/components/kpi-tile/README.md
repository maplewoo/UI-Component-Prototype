# KpiTile（关键指标磁贴）

一个只展示的迷你统计卡：顶部一条 3px 语义色条，下方小标签 + 强调数值。用于在看板/工作台顶部并列展示几个关键数字（如"待处理 / 已完成 / 逾期"）。

- 解决的问题：把"一个数字 + 一个语义色"做成统一、紧凑、可并排的磁贴，避免每页各写一套小卡片样式。
- 典型场景：项目选择卡右侧指标区、看板 KPI 概览行、详情页指标块。
- 宜搭兼容版本：是（`01-Yida-Compatible`）。用普通函数式 React + 纯 CSS，可直接放进 YidaCodeCanvas 页面源码。
- 当前版本：v0.1
