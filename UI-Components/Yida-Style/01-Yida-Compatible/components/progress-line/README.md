# ProgressLine（完成率进度条）

一行式进度条：左侧文字标签 + 中间圆角胶囊进度条 + 右侧大号百分比。填充色与百分比文字同源（一个 `--pc` 变量）。用于概览"完成率 / 通过率 / 完成度"。

- 解决的问题：把"一条线 + 一个百分数"做成统一、可配色的横向进度条，避免各处进度条粗细/取色/数字样式不一。
- 典型场景：判定统计卡顶部的完成率、进度概览区块。
- 宜搭兼容版本：是（`01-Yida-Compatible`）。纯 DOM + CSS。
- 当前版本：v0.1
